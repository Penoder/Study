package com.penoder.study.kotlin.intelligentTransformation

/**
 * 返回 与 跳转
 * Kotlin 有三种结构化跳转表达式：return、break、continue
 *      return。默认从最直接包围它的函数或者匿名函数返回。（匿名函数如下注释的无方法名的方法）
 *      break。终⽌最直接包围它的循环。
 *      continue。继续下⼀次最直接包围它的循环
 *
 * Created by dell on 2017/9/8.
 */
class BreakEtc {

    /**
     * 如下 两个没有方法名的方法 被语法中称为 匿名函数，
     * 但是直接报错了呀 Function declaration must have a name
     */
//    fun(x: Int, y: Int): Int = x + y

//    fun(x: Int): Int {
//        return 0
//    }

}